// O erro se encontra na linha "distancia = Math.pow(Math.pow(x2-x1, 2) + Math.pow(y2-y1, 2), 1/2);" //
// onde o calculo está sendo por 1/2, ou seja, isso faz com que seja retornado um valor inteiro //
// sendo assim igual a 1.0 //